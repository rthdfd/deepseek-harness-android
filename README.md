# DSH Android

Unofficial Android client for DeepSeek Harness.

The app manages a local Termux runtime and opens the DSH Web UI on Android 11 or later.

Builds are produced by GitHub Actions. See `.github/workflows/android.yml`.
